---
import { _transition } from '../scripts/snips'

const {
    href,
    class: classes, ...attrs
} = Astro.props
---
<a
    href={ href }
    class:list={[
        'flex items-center',
        _transition(),
        classes
    ]}
    { ...attrs }>
    <slot/>
</a>