---
import Icon from '@atoms/Icon.astro'

const {
	text = 'New',
	base = true,
	theme = 'default',
	icon,
	class: classes,
	...props
} = Astro.props
---

<span
	class:list={[
		'flex items-center justify-center font-semibold uppercase text-xs py-1.5 px-3.5 rounded-full max-w-28 tracking-wide',
		theme === 'default' &&
			theme !== 'neutral' &&
			'bg-[var(--r-accent)] text-[var(--r-accent-contrast-text)]',
		theme === 'neutral' && 'bg-slate-500/30 text-slate-400',
		theme === 'positive' && 'bg-blue-500/30 text-blue-400',
		classes,
	]}
	{...props}>
	{icon && <Icon name={icon} class:list='w-5' />}
	<span set:text={text} />
</span>
