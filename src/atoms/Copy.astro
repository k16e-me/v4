---
const {
		copy,
		size = 'default',
		color = 'light',
		tag = 'p',
		class: classes,
		...props
	} = Astro.props,
	Tag = tag
---

<Tag
	set:text={copy}
	class:list={[
		color == 'light' && 'text-[var(--r-contrast-text)]/70',
		color == 'dark' && 'text-[var(--r-primary)]/70',
		color == 'accent' && 'text-[var(--r-accent)]',
		size == 'sm' && 'text-sm',
		classes,
	]}
	{...props}
/>
