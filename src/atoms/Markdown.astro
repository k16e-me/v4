---
import { marked } from 'marked'
import Prose from './Prose.astro'

const { post, content, class: classes, ...props } = Astro.props
---

<Prose post={post}>
	<Fragment set:html={marked.parse(content)} />
</Prose>
