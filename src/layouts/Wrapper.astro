---
const
    {
        x = true,
        tag = 'div',
        class: classes, ...props
    } = Astro.props,
    Tag = tag
---
<Tag
    { ...props }
    class:list={[
        'grid grid-cols-12',
        x && 'px-4',
        classes
    ]}>
    <slot/>
</Tag>