---
const
    {
        component = null,
        padding = 'both',
        title,
        tag = 'section',
        class: classes,
        ...attrs
    } = Astro.props,
    Tag = tag
---
<Tag
    class:list={[
        `_section _section-${ component }`,
        padding === 'both' && 'py-16 sm:py-20',
        padding === 'bottom' && 'pb-16 sm:pb-20',
        classes
    ]} { ...attrs }>
    { title && <h2 set:text={ title } class="sr-only"/> }
    <slot/>
</Tag>