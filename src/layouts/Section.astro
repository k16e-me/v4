---
const
    {
        component = null,
        padding = 'both',
        title,
        class: classes,
        ...attrs
    } = Astro.props
---
<section
    class:list={[
        `_section _section-${ component } relative`,
        padding === 'both' && 'py-14 sm:py-20 lg:py-24',
        padding === 'bottom' && 'pb-14 sm:pb-20 lg:pb-24',
        classes
    ]} { ...attrs }>
    { title && <h2 set:text={ title } class="sr-only"/> }
    <slot/>
</section>