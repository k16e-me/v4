---
const
    {
        component = null,
        padding = true,
        title,
        class: classes,
        ...attrs
    } = Astro.props
---
<section
    class:list={[
        `_section _section-${ component } relative`,
        padding && 'py-14 sm:py-20 lg:py-24',
        classes
    ]} { ...attrs }>
    { title && <h2 set:text={ title } class="sr-only"/> }
    <slot/>
</section>