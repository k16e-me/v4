---
import Wrapper from './Wrapper.astro'
import Icon from '@atoms/Icon.astro'
import nav from '@content/settings/nav.json'
import _slugify from '@scripts/slugify'
import AnchorNav from '@atoms/AnchorNav.astro'

const
    {
        title,
        class: classes, ...attrs
    } = Astro.props
---
<header
    class:list={[
        'z-50 fixed top-0 inset-x-0',
        classes
    ]}
    data-header-main
    { ...attrs }>
    <Wrapper
        tag="nav"
        padX={ false }
        class:list="fixed bottom-0 inset-x-0">
        <ul class="flex items-center justify-between xs:justify-start [&>*]:w-full [&>*]:xs:w-auto">
            <li>
                <AnchorNav
                    href="/"
                    data="/"
                    text="K16E, Inc."
                    showIcon
                    showText={ false }
                    icon="logo"
                />
            </li>
            { nav.map(it => (
                <li>
                    <AnchorNav
                        href={ `/${it.slug }`}
                        text={ it.title }
                        data={ _slugify(it.slug) }
                    />
                </li>
            )) }
        </ul>
    </Wrapper>
</header>