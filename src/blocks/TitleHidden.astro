---
const
    { title, tag = 'h1', class: classes, ...props }
    = Astro.props,
    Tag = tag
---
<Tag
    class:list={['sr-only', classes]}
    aria-label={ title }
    {...props}>
	{ title }
</Tag>